extends layout

block content

  .jumbotron

  .container

    h1= title

    each val in properties
      .panel.panel-default
        .panel-heading
          h3.panel-title= val.dataValues.Name + " - £" + val.dataValues.price + " per night"
        .panel-body
          p= val.dataValues.description
          a(href="properties/" + val.dataValues.id) view property
